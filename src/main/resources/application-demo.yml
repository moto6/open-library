spring:

  application:
    name: open-library

  config:
    activate:
      on-profile: test

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:18080
    username: root
    password: hardpaaswordplz

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: false
        #show_sql: true
        format_sql: true
        use_sql_comments: true



